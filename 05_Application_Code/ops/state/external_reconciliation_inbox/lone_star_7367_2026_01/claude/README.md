# Claude originals were never deposited here

This `claude/` directory is the **only** place Cursor can checksum Claude’s Lone Star January package.

As of 2026-08-12, Claude’s six files (including `MANIFEST.sha256` and `source_manifest.csv`) are **not** in this git repository.

Do **not** recreate those files and represent them as originals.

If the files still exist, they are on Jerry’s Windows OneDrive tree:

`C:\Users\jerry\OneDrive\Cohen Entity Accounting v2\05_Application_Code\ops\state\external_reconciliation_inbox\lone_star_7367_2026_01\`

That path is not this repo. Copy-with-commit into **this** `claude/` folder (unchanged bytes) is the only valid deposit.
