services:
  - type: web
    name: ljc-accounting-app
    env: node
    plan: free
    buildCommand: npm install && npm run build:frontend
    startCommand: NODE_ENV=production node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: ljc-accounting-secret-key-2026
      - key: PORT
        value: 3000
    routes:
      - path: /
        matchType: prefix
    healthCheckPath: /health
